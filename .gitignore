# Large terrain and shadow data files (keep local only)
# These files are too large for GitHub (some over 10GB)

# Weather data collection files (large datasets)
resources/meteo_api/weather_data_collection.json
# 3-hour aggregated file is too large for GitHub (165MB)
resources/meteo_api/weather_data_3hour.json

# Add original 3-hour file (over 100MB, must not be tracked)
resources/meteo_api/weather_data_3hour_original.json

# Shadow data files
resources/shadows/*.tif

# Hillshade data files (allow 100m, exclude larger resolutions)
resources/hillshade/*25m*.tif
resources/hillshade/*5m*.tif

# Large terrain files (allow 100m, exclude larger resolutions)
resources/terrains/*25m*.tif
resources/terrains/*5m*.tif
resources/terrains/DGM_Tirol_*.tif
resources/terrains/*.tfw

# Allow 100m resolution files for frontend use
!resources/hillshade/*100m*.tif
!resources/terrains/*100m*.tif

# Node modules
node_modules/

# OS files
.DS_Store

# Local data cache
*.cache
